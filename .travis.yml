os: linux
language: cpp

dist: bionic

compiler:
    - gcc
    - clang

branches:
    only: 
    - /.*/

install:
    - ./install_cmake.sh
script:
    - mkdir build && cd build
    - /usr/local/bin/cmake ..
    - /usr/local/bin/cmake --build  .
    - fetchContent_example
